import { formatDate } from '../../utils/dateHelpers';
export function SalarySlipCard({ slip, onStatusChange, updating }) { return <article className="salary-card"><span className="eyebrow">{slip.month}</span><h3>₹{Number(slip.netPay).toLocaleString()}</h3><p>Base ₹{Number(slip.baseSalary).toLocaleString()} · Allowances ₹{Number(slip.allowances).toLocaleString()}</p><div className="salary-payment-meta"><span className={`badge ${slip.status}`}>{slip.status}</span>{slip.paidOn && <span className="salary-paid-date">Paid on {formatDate(slip.paidOn)}</span>}</div>{onStatusChange && <label className="salary-status-control">Payment status<select value={slip.status === 'paid' ? 'paid' : 'issued'} disabled={updating} onChange={(event) => onStatusChange(slip, event.target.value)}><option value="issued">Not paid</option><option value="paid">Paid</option></select></label>}</article>; }

