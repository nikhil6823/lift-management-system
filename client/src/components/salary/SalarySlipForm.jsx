import { useState } from 'react';
export function SalarySlipForm({ employees, onSave }) { const [form, setForm] = useState({ employee: '', month: new Date().toISOString().slice(0, 7), baseSalary: '', allowances: 0, deductions: 0, status: 'issued' }); const [saving, setSaving] = useState(false); const change = (e) => setForm({ ...form, [e.target.name]: e.target.value }); const submit = async (e) => { e.preventDefault(); setSaving(true); try { await onSave({ ...form, baseSalary: Number(form.baseSalary), allowances: Number(form.allowances), deductions: Number(form.deductions) }); } finally { setSaving(false); } }; return <form className="form-grid compact-form" onSubmit={submit}><label>Employee<select required name="employee" value={form.employee} onChange={change}><option value="">Select employee</option>{employees.map((item) => <option key={item._id} value={item._id}>{item.user?.name}</option>)}</select></label><label>Month<input required type="month" name="month" value={form.month} onChange={change} /></label><label>Base salary<input required type="number" min="0" name="baseSalary" value={form.baseSalary} onChange={change} /></label><label>Allowances<input type="number" min="0" name="allowances" value={form.allowances} onChange={change} /></label><label>Deductions<input type="number" min="0" name="deductions" value={form.deductions} onChange={change} /></label><div className="form-actions"><button disabled={saving}>{saving ? 'Creating…' : 'Create slip'}</button></div></form>; }
